{% extends 'base.html.twig' %}

{% block body %}

<style>
    #titre{
        text-decoration: underline;
    }
    #test{
        width: 25vw;
        display:block;
    }
</style>

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">

                <a href="{{ path('get_all_users') }}"><button type="button" class="btn btn-light btn-fw">Retour</button> </a>               

                <div class="container">

                    <h2 id="titre"> Ajout d'un formateur</h2>
                    <br>

                    <div id="test">
                    {{ form(form) }}
                    </div>

                <div>
            </div>
        </div>
    </div>
</div>

{% endblock %}